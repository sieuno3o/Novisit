{
  "name": "novisit",
  "version": "1.0.0",
  "description": "Novisit - A full-stack web application with React, Node.js, Express, BullMQ, Redis, and MongoDB",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd server && npm run dev",
    "client:dev": "cd client && npm run dev",
    "build": "npm run client:build && npm run server:build",
    "client:build": "cd client && npm run build",
    "server:build": "cd server && npm run build",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f"
  },
  "keywords": [
    "react",
    "nodejs",
    "express",
    "bullmq",
    "redis",
    "mongodb",
    "docker"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.10",
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "overrides": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "private": true,
  "dependencies": {
    "axios": "^1.12.2",
    "cors": "^2.8.5",
    "discord.js": "^14.24.2",
    "jsonwebtoken": "^9.0.2",
    "lucide-react": "^0.548.0",
    "react-icons": "^5.5.0"
  }
}
