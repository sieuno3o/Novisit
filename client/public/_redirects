# '/api/'로 시작하는 모든 요청을, '/api/'를 *떼고* 백엔드로 보냄
/api/* http://13.238.219.0:5000/:splat    200

# '/auth/' 요청은 '/auth/'를 *붙여서* 백엔드로 보냄 (refresh API 때문)
/auth/* http://13.238.219.0:5000/auth/:splat   200

# 나머지는 모두 React 앱으로
/* /index.html                           200